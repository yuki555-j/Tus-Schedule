export type Works = {
  name: string;
  limit: string;
  url: string;
};

export type News = {
  title: string
  date: string
  url: string
  user: string
}

export type User = {
  username: string
  password: string
}
